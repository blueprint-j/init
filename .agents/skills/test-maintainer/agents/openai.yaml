interface:
  display_name: "Test Maintainer"
  short_description: "Keep tests aligned with the repository strategy."
  default_prompt: "Use $test-maintainer to update these tests while preserving the repository's testing strategy."
