---
name: domain-module-maintainer
description: Use when maintaining an existing domain module or adding a new supported module. Review module registration, query, presentation, detail flow, product catalog, and related tests so runtime and documentation stay aligned.
---

# Domain Module Maintainer

Use this skill when a supported domain module changes.
The canonical behavior guide for this skill is `docs/agent-rules/domain-module-maintainer.md`.

## Read In Order

1. Read `AGENTS.md`.
2. Read `docs/agent-rules/agent-protocol.md`.
3. Read `docs/agent-rules/domain-module-maintainer.md`.
4. Then read only the concrete module docs and code touched by the request.

## Note

- Keep this skill thin. If the maintenance guidance needs to change, update `docs/agent-rules/domain-module-maintainer.md`.
